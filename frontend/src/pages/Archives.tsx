import {useEffect,useState} from 'react';import {ArchiveRestore,RotateCcw} from 'lucide-react';import {api} from '../lib/api';import {Layout} from '../components/Layout';
export function Archives(){const [rows,setRows]=useState<any[]>([]),[message,setMessage]=useState('');const load=()=>api<any[]>('/archives/assets').then(setRows);useEffect(()=>{load()},[]);async function restore(id:string){try{await api('/assets/'+id+'/restore',{method:'POST'});setMessage('Actif restauré');load()}catch(x:any){setMessage(x.message)}}return <Layout title="Actifs archivés"><article className="panel tablePanel"><table><thead><tr><th>IP</th><th>Nom</th><th>Constructeur</th><th>Type</th><th>Motif</th><th>Archivé le</th><th>Action</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td className="mono">{x.addresses.join(', ')}</td><td>{x.hostname||'—'}</td><td>{x.manufacturer||'—'}</td><td>{x.device_type}</td><td>{x.reason||'—'}</td><td>{new Date(x.archived_at).toLocaleString('fr')}</td><td><button className="button" onClick={()=>restore(x.id)}><RotateCcw/>Restaurer</button></td></tr>)}</tbody></table>{!rows.length&&<div className="empty"><ArchiveRestore/>Aucun actif archivé</div>}</article>{message&&<div className="panel" style={{marginTop:12}}>{message}</div>}<p className="hint">Un actif archivé est aussi restauré automatiquement lorsqu’un scan le redécouvre.</p></Layout>}
