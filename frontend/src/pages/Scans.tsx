import {FormEvent,useEffect,useState} from 'react';import {Play,ScanSearch} from 'lucide-react';import {api} from '../lib/api';import {Layout} from '../components/Layout';
export function Scans(){const [scans,setScans]=useState<any[]>([]),[profiles,setProfiles]=useState<any[]>([]),[credentials,setCredentials]=useState<any[]>([]),[error,setError]=useState('');const load=()=>{api<any[]>('/scans').then(setScans);api<any[]>('/scan-profiles').then(setProfiles);api<any[]>('/credentials').then(setCredentials).catch(()=>setCredentials([]))};useEffect(load,[]);async function submit(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api('/scans',{method:'POST',body:JSON.stringify({target:f.get('target'),profile_id:f.get('profile_id'),credential_id:f.get('credential_id')||null})});setError('');load()}catch(x:any){setError(x.message)}}return <Layout title="Scans de découverte"><div className="split"><article className="panel formPanel"><h3>Nouveau scan</h3><form onSubmit={submit}><label>Cible autorisée<input name="target" placeholder="10.0.10.0/24" required/></label><label>Profil<select name="profile_id" required>{profiles.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><label>Identifiant SNMPv3<select name="credential_id"><option value="">Aucun</option>{credentials.map(c=><option key={c.id} value={c.id}>{c.name}</option>)}</select></label>{error&&<div className="error">{error}</div>}<button className="primary"><Play/>Lancer le scan</button></form><p className="hint">Le profil Infrastructure SNMPv3 exige un identifiant. Les secrets ne sont jamais renvoyés.</p></article><article className="panel"><h3>Historique</h3><div className="scanList">{scans.map(s=><div key={s.id}><ScanSearch/><span><b>{s.target}</b><small>{new Date(s.created_at).toLocaleString('fr')}{s.error?' · '+s.error:''}</small></span><em className={'badge '+s.status}>{s.status}</em></div>)}{!scans.length&&<div className="empty">Aucun scan</div>}</div></article></div></Layout>}
