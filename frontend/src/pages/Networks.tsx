import {FormEvent,useEffect,useState} from 'react';import {Globe2,Plus,Trash2} from 'lucide-react';import {api} from '../lib/api';import {Layout} from '../components/Layout';
export function Networks(){const [rows,setRows]=useState<any[]>([]),[error,setError]=useState(''),[adding,setAdding]=useState(false);const load=()=>api<any[]>('/networks').then(setRows).catch((e:any)=>setError(e.message));useEffect(()=>{void load()},[]);async function create(e:FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);try{await api('/networks',{method:'POST',body:JSON.stringify({cidr:f.get('cidr'),name:f.get('name'),state:f.get('state')})});setError('');setAdding(false);load()}catch(x:any){setError(x.message)}}async function remove(x:any){if(!confirm(`Supprimer le réseau ${x.cidr} ?\n\nS'il est lié à l'IPAM, ses adresses IPAM seront retirées. Les équipements découverts et les scans seront conservés.`))return;try{await api(`/networks/${x.id}?purge_ipam=true`,{method:'DELETE'});setError('');load()}catch(e:any){setError(e.message)}}return <Layout title="Réseaux enregistrés"><div className="pageTools"><div><p className="hint">Périmètres autorisés pour les scans et préfixes gérés dans l’IPAM.</p></div><button className="primary" onClick={()=>setAdding(!adding)}><Plus/> Ajouter un réseau</button></div>{adding&&<article className="panel assetEditor"><form onSubmit={create}><label>Réseau CIDR<input name="cidr" placeholder="192.168.10.0/24" required/></label><label>Nom<input name="name" placeholder="LAN utilisateurs" required/></label><label>État<select name="state"><option value="authorized">Autorisé</option><option value="discovered">Découvert</option><option value="disabled">Désactivé</option></select></label><button className="primary">Enregistrer</button></form></article>}{error&&<div className="error">{error}</div>}<article className="panel tablePanel"><table><thead><tr><th>État</th><th>Réseau</th><th>Nom</th><th>Dernier scan</th><th>Actions</th></tr></thead><tbody>{rows.map(x=><tr key={x.id}><td><span className={'badge '+x.state}>{x.state}</span></td><td className="mono">{x.cidr}</td><td>{x.name}</td><td>{x.last_scan_at?new Date(x.last_scan_at).toLocaleString('fr'):'Jamais'}</td><td><div className="rowActions"><button className="danger" title="Supprimer" onClick={()=>remove(x)}><Trash2/></button></div></td></tr>)}</tbody></table>{!rows.length&&<div className="empty"><Globe2/>Aucun réseau configuré</div>}</article></Layout>}
